#!/bin/bash

# ensure sudo

# prompt for credentials and save to env file

# install dependencies (docker, docker-compose, rust, cargo, etc.)

# start containers

    # validate interface for zeek

# direct user to run the parsers via the parser.sh script